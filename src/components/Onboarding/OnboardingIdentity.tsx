import React from 'react';
import Onboarding from './Onboarding';
import { onboardingConfigs } from './Onboarding.config';

function OnboardingIdentity() {
  // const config = onboardingConfigs[0];
  // return <Onboarding heading={config.heading} data={config.data} isBack={false} id={config.id} />;
}

export default OnboardingIdentity;
