import React from 'react';
import Onboarding from './Onboarding';
import { onboardingConfigs } from './Onboarding.config';

function FirstStepOnboarding() {
  // const config = onboardingConfigs[1];
  // return <Onboarding heading={config.heading} data={config.data} isBack={true} id={config.id} />;
}

export default FirstStepOnboarding;
